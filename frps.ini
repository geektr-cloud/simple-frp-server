[common]
bind_addr = ${BIND_ADDR}
bind_port = ${BIND_TCP_PORT}
kcp_bind_port = ${BIND_TCP_PORT}
bind_udp_port = ${BIND_UDP_PORT}
tcp_mux = true
max_pool_count = ${MAX_POOL_COUNT}
privilege_token = ${PRIVILEGE_TOKEN}

dashboard_addr = ${DASHBOARD_ADDR}
dashboard_port = ${DASHBOARD_PORT}
dashboard_user = ${DASHBOARD_USER}
dashboard_pwd = ${DASHBOARD_PASSWD}
